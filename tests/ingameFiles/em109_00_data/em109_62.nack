importactions TobiKadachi as tobikadachi

def node_000
	if self.current_quest().is_rank(rank.MR) 
		if function#104(1) 
			if function#101() 
				-> tobikadachi.climb_super_charge_start() 
			endif 
		else 
			if function#101() 
				-> tobikadachi.climb_charge_max() 
			else 
				-> tobikadachi.charge_max() 
			endif 
		endif 
	else 
		if function#101() 
			-> tobikadachi.climb_charge_max() 
		else 
			-> tobikadachi.charge_max() 
		endif 
	endif 
	conclude 
endf 

