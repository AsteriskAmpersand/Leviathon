importactions KuluYaKu as kuluyaku
importlibrary Global as Global

def node_000 : 1
	self.targetEnemy(33) 
	if function#41() 
		>> node_006 
	elif self.vertical_distance_to_target().gt(3000) 
		>> node_003 
	elif self.target_is(em.Rathian) 
		>> node_004 
	elif self.targetEnemy(33) 
		>> node_005 
	endif 
	conclude 
endf 

def node_001 : 2
	if self.distance_3d_to_target().leq(1500) 
		>> Global.node_057 
	elif self.angle_2d_cw_between(40,320) 
		>> Global.node_070 
	else 
	endif 
	return 
endf 

def node_002 : 3
	if self.distance_3d_to_target().leq(1000) 
		>> Global.node_057 
	else 
	endif 
	return 
endf 

def node_003 : 4
	if self.target_is(em.Rathian) 
		>> Global.node_049 
	else 
	endif 
	return 
endf 

def node_004 : 5
	if function#101(3) 
		if self.distance_3d_to_target().leq(3000) 
			>> node_001 
			-> kuluyaku.carry_sniff_s() 
			if function#101(1) 
				-> kuluyaku.surprise_drop_egg() 
			else 
			endif 
		else 
			-> kuluyaku.carry_sniff_l() 
		endif 
	else 
		if self.distance_3d_to_target().leq(3000) 
			-> kuluyaku.sniff_s() 
			-> kuluyaku.threat_short() 
		else 
			-> kuluyaku.sniff_l() 
		endif 
	endif 
	return 
endf 

def node_005 : 7
	if function#101(3) 
		-> kuluyaku.carry_sniff_l() 
	else 
		-> kuluyaku.sniff_l() 
	endif 
	return 
endf 

def node_006 : 8
	if function#101(3) 
		-> kuluyaku.carry_quick_search() 
	else 
		-> kuluyaku.quick_search() 
	endif 
	return 
endf 

