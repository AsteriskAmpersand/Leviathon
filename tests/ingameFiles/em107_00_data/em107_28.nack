importactions KuluYaKu as kuluyaku
importlibrary Global as Global

def node_000
	if [RegisterVar0 -= 15] 
		[RegisterVar0 ++] 
		conclude 
	else 
	endif 
	[RegisterVar0 |-] 
	>> node_001 
	repeat 
	conclude 
endf 

def node_001
	>> Global.node_024 
	self.targetEnemy(target_em.random_player_or_cat) 
	if self.distance_3d_to_target().leq(3500) 
		>> node_002 
	else 
		>> node_003 
	endif 
	return 
endf 

def node_002
	>> Global.node_069 
	if self.enraged() 
		>> Global.node_063 
	elif self.fatigued() 
		>> Global.node_067 
	else 
		>> Global.node_062 
	endif 
	return 
endf 

def node_003
	self.targetEnemy(58) 
	random (10) >> node_004 
	elser (90) 
	endr 
	self.targetEnemy(22) @ parameter2:2000 
	random (40) >> Global.node_069 
	elser (60) 
	endr 
	random (70) >> Global.node_059 
	elser (30) >> Global.node_062 
	endr 
	return 
endf 

def node_004
	if function#101(3) 
		if self.fatigued() 
			-> kuluyaku.carry_tired_walk(2,1,0,1500,800) 
		else 
			-> kuluyaku.carry_walk(2,1,0,1500,800) 
		endif 
	else 
		if self.fatigued() 
			-> kuluyaku.tired_walk(2,1,0,1500,800) 
		else 
			-> kuluyaku.walk(2,1,0,1500,800) 
		endif 
	endif 
	return 
endf 

