importactions Zinogre as zinogre
importlibrary Global as Global

def node_000
	function#103() 
	>> Global.node_004 
	>> Global.node_006 
	>> Global.node_003 
	>> node_001 
	reset 
endf 

def node_001
	self.targetEnemy(48) 
	if self.in_map(st.Challenge_Arena).in_area(-1) 
		>> node_004 
	elif self.distance_3d_to_target().leq(3500) 
		>> node_002 
	else 
		>> node_003 
	endif 
	return 
endf 

def node_002
	if self.target(7) 
	elif self.target(10) 
	else 
		>> Global.node_035 
	endif 
	if self.fatigued() 
		random (40) >> Global.node_180 
		elser (25) >> Global.node_184 
		elser (20) >> Global.node_176 
		elser (15) >> Global.node_177 
		endr 
	else 
		random (50) >> Global.node_184 
		elser (30) >> Global.node_176 
		elser (20) >> Global.node_177 
		endr 
	endif 
	return 
endf 

def node_003
	if self.targetEnemy(target_em.any_monster) 
		random (20) 
		elser (80) >> Global.node_035 
		endr 
	else 
	endif 
	random (30) >> Global.node_184 
	elser (15) >> Global.node_176 
	elser (20) >> Global.node_177 
	elser (15) >> Global.node_178 
	elser (20) >> Global.node_179 
	endr 
	return 
endf 

def node_004
	>> Global.node_035 
	random (40) >> Global.node_184 
	elser (30) >> Global.node_179 
	elser (30) >> Global.node_166 
	endr 
	return 
endf 

