importactions Zinogre as zinogre

def node_000 : 1
	function#103() 
	self.targetEnemy(target_em.closest_entity) 
	if self.target_is(em.Brachydios) 
		>> node_001 
	elif self.target_is(em.Rathalos) 
		>> node_002 
	elif self.target_is(em.Azure_Rathalos) 
		>> node_002 
	elif self.target_is(em.Yian_Garuga) 
		>> node_003 
	elif self.target_is(em.Scarred_Yian_Garuga) 
		>> node_003 
	elif self.target_is(em.Glavenus) 
		>> node_004 
	elif self.target_is(em.Acidic_Glavenus) 
		>> node_004 
	elif self.target_is(em.Fulgur_Anjanath) 
		>> node_005 
	endif 
	conclude 
endf 

def node_001 : 27
	if self.distance_3d_to_target().gt(1600) 
		-> zinogre.dash(1,0,0,1500) 
	endif 
	-> zinogre.grapple_with_em063_first() 
	return 
endf 

def node_002 : 28
	if self.distance_3d_to_target().gt(1600) 
		-> zinogre.dash(1,0,0,1500) 
	endif 
	-> zinogre.grapple_with_em002_first() 
	return 
endf 

def node_003 : 31
	if self.distance_3d_to_target().gt(1600) 
		-> zinogre.dash(1,0,0,1500) 
	endif 
	-> zinogre.grapple_with_em018_first() 
	return 
endf 

def node_004 : 29
	if self.distance_3d_to_target().gt(1600) 
		-> zinogre.dash(1,0,0,1500) 
	endif 
	-> zinogre.grapple_with_em080_first() 
	return 
endf 

def node_005 : 30
	if self.distance_3d_to_target().gt(1600) 
		-> zinogre.dash(1,0,0,1500) 
	endif 
	-> zinogre.grapple_with_em100_01_first() 
	return 
endf 

