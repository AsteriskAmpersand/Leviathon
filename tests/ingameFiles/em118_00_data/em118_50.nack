importactions Bazelgeuse as bazelgeuse

def node_000
	>> node_001 
	conclude 
endf 

def node_001
	self.targetEnemy(target_em.closest_entity) 
	if self.target_is(em.Deviljho) 
		>> node_003 
	else 
	endif 
	return 
endf 

def node_002
	if self.flying() 
		if function#AF(5000) 
			-> bazelgeuse.landing() 
		else 
			-> bazelgeuse.threat_short_fly() 
		endif 
	endif 
	return 
endf 

def node_003
	if function#104() 
	else 
		>> node_002 
	endif 
	if self.distance_3d_to_target().gt(1350) 
		*&
		-> bazelgeuse.dash(1,0,0,1250) 
	else 
	endif 
	if self.distance_3d_to_target().leq(800) 
		-> bazelgeuse.back_turn() 
	else 
		-> bazelgeuse.quick_turn() 
	endif 
	-> bazelgeuse.grapple_with_em043_start() 
	return 
endf 

