importactions Stygian_Zinogre as stygian_zinogre
importlibrary Global as Global

def node_000
	function#103() 
	>> Global.node_006 
	>> Global.node_003 
	>> node_001 
	reset 
endf 

def node_001
	self.targetEnemy(48) 
	if self.in_map(st.Challenge_Arena).in_area(-1) 
		>> node_004 
	elif self.distance_3d_to_target().leq(3500) 
		>> node_002 
	else 
		>> node_003 
	endif 
	return 
endf 

def node_002
	if self.target(7) 
	elif self.target(10) 
	else 
		>> Global.node_037 
	endif 
	if self.fatigued() 
		random (40) >> Global.node_217 
		elser (25) >> Global.node_221 
		elser (20) >> Global.node_213 
		elser (15) >> Global.node_214 
		endr 
	else 
		random (50) >> Global.node_221 
		elser (30) >> Global.node_213 
		elser (20) >> Global.node_214 
		endr 
	endif 
	return 
endf 

def node_003
	if self.targetEnemy(target_em.any_monster) 
		random (20) 
		elser (80) >> Global.node_037 
		endr 
	else 
	endif 
	random (30) >> Global.node_221 
	elser (15) >> Global.node_213 
	elser (20) >> Global.node_214 
	elser (15) >> Global.node_215 
	elser (20) >> Global.node_216 
	endr 
	return 
endf 

def node_004
	>> Global.node_037 
	random (40) >> Global.node_221 
	elser (30) >> Global.node_216 
	elser (30) >> Global.node_203 
	endr 
	return 
endf 

