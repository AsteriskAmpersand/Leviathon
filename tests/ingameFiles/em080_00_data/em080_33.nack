importactions Glavenus as glavenus

def node_000
	self.targetEnemy(target_em.closest_entity) 
	if self.distance_3d_to_target().leq(1000) 
		-> glavenus.back_turn() 
	elif self.angle_2d_cw_between(30,330) 
		-> glavenus.turn() 
	endif 
	if self.target(13) 
		-> glavenus.walk() 
		-> glavenus.predator_eat() 
	else 
	endif 
	conclude 
endf 

