importactions Glavenus as glavenus
importlibrary Global as Global

def node_000 : 1
	if function#41() 
		>> Global.node_021 => reset 
	else 
	endif 
	>> Global.node_001 
	>> Global.node_022 
	>> node_001 => reset 
	reset 
endf 

def node_001 : 2
	self.targetEnemy(48) 
	if self.in_map(st.Challenge_Arena).in_area(-1) 
		>> node_004 
	elif self.distance_3d_to_target().leq(3500) 
		>> node_002 
	else 
		>> node_003 
	endif 
	return 
endf 

def node_002 : 3
	>> Global.node_009 
	if self.fatigued() 
		-> glavenus.tired_wait() 
	else 
		random (40) -> glavenus.wait() 
		elser (60) -> glavenus.sniff_s() 
		endr 
	endif 
	return 
endf 

def node_003 : 4
	if self.targetEnemy(target_em.any_monster) 
		random (20) >> Global.node_008 
		elser (80) 
		endr 
	else 
	endif 
	random (20) -> glavenus.combat_search() 
	elser (35) -> glavenus.anger_search() 
	elser (10) -> glavenus.sniff_s() 
	elser (20) -> glavenus.sniff_l() 
	elser (15) -> glavenus.wait() 
	endr 
	return 
endf 

def node_004 : 23
	>> Global.node_008 
	random (30) -> glavenus.wait() 
	elser (25) -> glavenus.sniff_s() 
	elser (10) -> glavenus.sniff_l() 
	elser (15) -> glavenus.combat_search() 
	elser (20) -> glavenus.threat() 
	endr 
	return 
endf 

