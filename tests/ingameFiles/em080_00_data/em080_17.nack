importactions Glavenus as glavenus

def node_000
	>> node_001 
	>> node_002 
	conclude 
endf 

def node_001
	if function#41() 
	else 
		if function#AB() 
			function#AA() 
			-> glavenus.threat() 
		else 
		endif 
	endif 
	return 
endf 

def node_002
	self.targetArea(14) 
	if self.distance_3d_to_target().gt(800) 
		>> node_004 
		repeat 
	else 
	endif 
	return 
endf 

def node_003
	self.targetEnemy(target_em.random_player_or_cat) 
	-> glavenus.idle() 
	if self.distance_3d_to_target().leq(800) 
		-> glavenus.back_turn() 
	elif self.angle_2d_cw_between(30,330) 
		-> glavenus.turn() 
	else 
	endif 
	return 
endf 

def node_004
	if self.fatigued() 
		-> glavenus.tired_dash(2,5,0,3000) 
	else 
		-> glavenus.dash(2,5,0,3000) 
	endif 
	return 
endf 

