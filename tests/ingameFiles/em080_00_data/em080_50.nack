importactions Glavenus as glavenus

def node_000 : 1
	self.targetEnemy(target_em.closest_entity) 
	if self.target_is(em.Rathalos) 
		>> node_001 
	elif self.target_is(em.Azure_Rathalos) 
		>> node_001 
	elif self.target_is(em.Diablos) 
		>> node_002 
	elif self.target_is(em.Black_Diablos) 
		>> node_002 
	elif self.target_is(em.Tigrex) 
		>> node_004 
	elif self.target_is(em.Brute_Tigrex) 
		>> node_004 
	elif self.target_is(em.Zinogre) 
		>> node_003 
	elif self.target_is(em.Stygian_Zinogre) 
		>> node_003 
	elif self.target_is(em.Odogaron) 
		>> node_005 
	elif self.target_is(em.Ebony_Odogaron) 
		>> node_005 
	endif 
	conclude 
endf 

def node_001 : 2
	if self.distance_2d_to_target().gt(1900) 
		-> glavenus.dash(6,0,0,1800) 
	endif 
	if self.distance_2d_to_target().leq(1100) 
		-> glavenus.back_turn() 
	else 
		-> glavenus.quick_turn() 
	endif 
	-> glavenus.grapple_with_em002_ver2_start() 
	return 
endf 

def node_002 : 3
	if self.distance_2d_to_target().gt(1800) 
		-> glavenus.dash(6,0,0,1770) 
	endif 
	if self.distance_2d_to_target().leq(1700) 
		-> glavenus.back_turn() 
		-> glavenus.dash(1,0,0,1770) 
	else 
		-> glavenus.quick_turn() 
	endif 
	-> glavenus.grapple_with_em007_start() 
	return 
endf 

def node_003 : 4
	if self.distance_2d_to_target().gt(1600) 
		-> glavenus.dash(6,0,0,1500) 
	endif 
	-> glavenus.grapple_with_em057_first() 
	return 
endf 

def node_004 : 5
	if self.distance_3d_to_target().gt(1600) 
		-> glavenus.dash(1,0,0,1500) 
	endif 
	-> glavenus.grapple_with_em032_const() 
	return 
endf 

def node_005 : 6
	if self.distance_2d_to_target().gt(1600) 
		-> glavenus.dash(6,0,0,1500) 
	endif 
	-> glavenus.grapple_with_em113_first() 
	return 
endf 

