importactions Rathalos as rathalos
importlibrary Global as Global

def node_000
	self.targetEnemy(target_em.closest_entity) 
	if self.target_is(em.Zinogre) 
		>> node_001 
	elif self.target_is(em.Brachydios) 
		>> node_002 
		conclude 
	endif 
	if self.distance_2d_to_target().gt(2000) 
		>> Global.node_034 
	endif 
	if function#AC() 
	else 
		>> Global.node_033 
	endif 
	if self.target_is(em.Anjanath) 
		-> rathalos.grapple_to_em100_start() 
	elif self.target_is(em.Odogaron) 
		-> rathalos.grapple_with_em113_first() 
	elif self.target_is(em.Ebony_Odogaron) 
		-> rathalos.grapple_with_em113_first() 
	elif self.target_is(em.Nargacuga) 
		-> rathalos.grapple_with_em037_first() 
	elif self.target_is(em.Tigrex) 
		-> rathalos.grapple_with_em032_first() 
	elif self.target_is(em.Glavenus) 
		-> rathalos.grapple_with_em080_ver1_const() 
	elif self.target_is(em.Acidic_Glavenus) 
		-> rathalos.grapple_with_em080_ver1_const() 
	elif self.target_is(em.Brute_Tigrex) 
		-> rathalos.grapple_with_em032_first() 
	endif 
	conclude 
endf 

def node_001
	if self.distance_2d_to_target().gt(1600) 
		-> rathalos.dash_no_hit(6,0,0,1500) 
	endif 
	-> rathalos.grapple_with_em057_first() 
	return 
endf 

def node_002
	if self.distance_2d_to_target().gt(1800) 
		-> rathalos.glide_combat(6,0,0,1800) 
	endif 
	if self.distance_2d_to_target().leq(1500) 
		-> rathalos.step_back_fly(6,0,0,1500) 
	endif 
	-> rathalos.grapple_with_em063_start() 
	return 
endf 

