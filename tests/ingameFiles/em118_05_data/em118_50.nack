importactions Seething_Bazelgeuse as seething_bazelgeuse

def node_000 : 1
	>> node_001 
	conclude 
endf 

def node_001 : 8
	self.targetEnemy(target_em.closest_entity) 
	if self.target_is(em.Savage_Deviljho) 
		>> node_003 
	else 
	endif 
	return 
endf 

def node_002 : 9
	if self.flying() 
		if function#AF(5000) 
			-> seething_bazelgeuse.landing() 
		else 
			-> seething_bazelgeuse.threat_fly() 
		endif 
	endif 
	return 
endf 

def node_003 : 10
	if function#104() 
	else 
		>> node_002 
	endif 
	if self.distance_3d_to_target().gt(1350) 
		-> seething_bazelgeuse.dash(1,0,0,1250) 
	else 
	endif 
	if self.distance_3d_to_target().leq(800) 
		-> seething_bazelgeuse.back_turn() 
	else 
		-> seething_bazelgeuse.quick_turn() 
	endif 
	-> seething_bazelgeuse.grapple_with_em043_start() 
	return 
endf 

