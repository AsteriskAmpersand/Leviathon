importactions Dodogama as dodogama
importlibrary Global as Global

def node_000 : 14
	>> Global.node_003 
	>> Global.node_002 
	>> node_001 => reset 
	reset 
endf 

def node_001 : 15
	self.targetEnemy(48) 
	if self.in_map(st.Challenge_Arena).in_area(-1) 
		>> node_004 
	elif self.distance_3d_to_target().leq(3500) 
		>> node_002 
	else 
		>> node_003 
	endif 
	return 
endf 

def node_002 : 16
	if self.target(7) 
	elif self.target(10) 
	else 
		>> Global.node_034 
	endif 
	if self.fatigued() 
		random (40) -> dodogama.tired() 
		elser (30) -> dodogama.anger_search() 
		elser (20) -> dodogama.smell() 
		elser (10) -> dodogama.smell_max() 
		endr 
	else 
		random (40) -> dodogama.wait() 
		elser (30) -> dodogama.anger_search() 
		elser (20) -> dodogama.smell() 
		elser (10) -> dodogama.smell_max() 
		endr 
	endif 
	return 
endf 

def node_003 : 17
	if self.targetEnemy(target_em.any_monster) 
		random (20) >> Global.node_034 
		elser (80) 
		endr 
	else 
	endif 
	random (20) -> dodogama.anger_search() 
	elser (35) -> dodogama.short_search() 
	elser (10) -> dodogama.smell() 
	elser (20) -> dodogama.smell_max() 
	elser (15) -> dodogama.wait() 
	endr 
	return 
endf 

def node_004 : 18
	>> Global.node_034 
	random (35) -> dodogama.wait() 
	elser (25) -> dodogama.anger_search() 
	elser (40) -> dodogama.threat_short() 
	endr 
	return 
endf 

