importactions Kulve_Taroth as kulve_taroth
importlibrary Global as Global

def node_000
	self.targetEnemy(target_em.random_player_or_cat) 
	if self.target(3) 
		if self.distance_2d_to_target().leq(3500) 
			>> Global.node_109 
		else 
		endif 
		>> Global.node_335 
	else 
		if self.vertical_distance_to_target().leq(400) 
			if self.distance_2d_to_target().leq(1200) 
				>> Global.node_333 
			elif self.distance_3d_to_target().leq(2200) 
				if self.target(10) 
					>> Global.node_333 
				else 
					>> Global.node_332 
					>> Global.node_328 
				endif 
			else 
				>> Global.node_332 
				>> Global.node_328 
			endif 
		else 
			if self.distance_2d_to_target().leq(3500) 
				>> Global.node_109 
			else 
			endif 
			>> Global.node_335 
		endif 
	endif 
	reset 
endf 

