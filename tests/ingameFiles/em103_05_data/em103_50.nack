importactions Ruiner_Nergigante as ruiner_nergigante

def node_000
	self.targetEnemy(target_em.closest_entity) 
	if self.target_is(em.Savage_Deviljho) 
		>> node_001 
	elif self.target_is(em.Velkhana) 
		>> node_002 
	elif self.target_is(em.Rajang) 
		>> node_003 
	elif self.target_is(em.Furious_Rajang) 
		>> node_003 
	else 
	endif 
	conclude 
endf 

def node_001
	if self.distance_3d_to_target().gt(2100) 
		-> ruiner_nergigante.dash(1,0,0,2000) 
	endif 
	if self.distance_3d_to_target().leq(1000) 
		-> ruiner_nergigante.back_turn() 
	else 
		-> ruiner_nergigante.quick_turn() 
	endif 
	-> ruiner_nergigante.grapple_with_em043_05_first() 
	return 
endf 

def node_002
	if self.distance_3d_to_target().gt(1100) 
		-> ruiner_nergigante.dash(1,0,0,1300) 
	endif 
	if self.distance_3d_to_target().leq(500) 
		-> ruiner_nergigante.back_turn() 
	else 
		-> ruiner_nergigante.quick_turn() 
	endif 
	-> ruiner_nergigante.grapple_with_em124_adjust_wait() 
	-> ruiner_nergigante.quick_turn() 
	-> ruiner_nergigante.grapple_with_em124_start() 
	return 
endf 

def node_003
	if self.distance_2d_to_target().gt(1400) 
		-> ruiner_nergigante.dash(6,0,0,1350) 
	endif 
	if self.distance_2d_to_target().leq(750) 
		-> ruiner_nergigante.back_turn() 
	else 
		-> ruiner_nergigante.quick_turn() 
	endif 
	-> ruiner_nergigante.grapple_with_em023_first() 
	return 
endf 

