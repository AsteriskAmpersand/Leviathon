importactions Jyuratodus as jyuratodus
importlibrary Global as Global

def node_000
	if self.current_quest().is_rank(rank.MR) 
		>> node_001 
	else 
		>> node_002 
	endif 
	reset 
endf 

def node_001
	self.targetEnemy(21) @ parameter2:750 
	>> node_009 
	if self.enraged() >> node_014 
	elif self.fatigued() >> node_015 
	endif >> node_016 
	reset 
endf 

def node_002
	self.targetEnemy(21) @ parameter2:750 
	>> node_009 
	if self.enraged() >> node_004 
	elif self.fatigued() >> node_005 
	endif >> node_003 
	reset 
endf 

def node_003
	random (35) >> node_006 
	elser (25) >> node_007 
	elser (10) >> node_008 
	elser (10) >> Global.node_053 
	elser (20) >> Global.node_068 
	endr 
	return 
endf 

def node_004
	random (30) >> node_006 
	elser (25) >> node_007 
	elser (15) >> node_008 
	elser (10) >> Global.node_053 
	elser (20) >> Global.node_068 
	endr 
	return 
endf 

def node_005
	random (35) >> node_006 
	elser (25) >> node_007 
	elser (10) >> Global.node_072 
	elser (10) >> Global.node_053 
	elser (20) >> Global.node_068 
	endr 
	return 
endf 

def node_006
	if function#101() >> Global.node_023 
	else >> Global.node_034 
	endif 
	return 
endf 

def node_007
	if function#101() >> Global.node_025 
	else >> Global.node_038 
	endif 
	return 
endf 

def node_008
	if function#101() >> Global.node_027 
	else >> Global.node_037 
	endif 
	return 
endf 

def node_009
	self.targetEnemy(target_em.random_player_or_cat) 
	self.target_on_part(0) >> node_010 => reset 
	self.target_on_part(1) >> node_011 => reset 
	return 
endf 

def node_010
	if self.mounted() -> jyuratodus.swim_ride_rage_head() 
	elif self.mounted() -> jyuratodus.swim_ride_rage_head() @ parameter1:1 
	endif 
	-> jyuratodus.swim_chance_idle() 
	return 
endf 

def node_011
	if self.mounted() -> jyuratodus.swim_ride_rage_body() 
	elif self.mounted() -> jyuratodus.swim_ride_rage_body_lv2() @ parameter1:1 
	endif 
	-> jyuratodus.swim_chance_idle() 
	return 
endf 

def node_012
	if function#3B() 
		if self.mount_staggered_twice() >> node_013 
		endif 
	else 
		if self.mount_stabbed() 
			random (50) >> node_013 
			elser (50) 
			endr 
		endif 
	endif 
	return 
endf 

def node_013
	if self.mounted() -> jyuratodus.swim_ride_tired_lv2() @ parameter1:1 
	endif 
	return 
endf 

def node_014
	random (40) >> node_006 
	elser (35) >> node_007 
	elser (25) >> node_008 
	elser (0) >> Global.node_053 
	elser (0) >> Global.node_068 
	endr 
	return 
endf 

def node_015
	random (50) >> node_006 
	elser (35) >> node_007 
	elser (0) >> Global.node_072 
	elser (15) >> Global.node_053 
	elser (0) >> Global.node_068 
	endr 
	return 
endf 

def node_016
	random (45) >> node_006 
	elser (30) >> node_007 
	elser (15) >> node_008 
	elser (10) >> Global.node_053 
	elser (0) >> Global.node_068 
	endr 
	return 
endf 

