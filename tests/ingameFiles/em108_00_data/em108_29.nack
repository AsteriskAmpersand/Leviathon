importactions Jyuratodus as jyuratodus
importlibrary Global as Global

def node_000
	>> Global.node_002 
	self.targetArea(19) >> node_001 
	self.targetEnemy(48) 
	if self.in_map(st.Challenge_Arena).in_area(-1) >> node_002 
	elif self.distance_3d_to_target().leq(3500) >> node_003 
	else >> node_004 
	endif 
	reset 
endf 

def node_001
	if self.distance_3d_to_target().gt(600) >> Global.node_056 
		repeat 
	endif 
	return 
endf 

def node_002
	>> Global.node_062 
	random (20) >> Global.node_073 
	elser (40) >> Global.node_068 
	elser (40) >> Global.node_053 
	endr 
	return 
endf 

def node_003
	>> Global.node_062 
	if self.fatigued() >> Global.node_072 
	else 
		random (50) >> Global.node_053 
		elser (50) >> Global.node_068 
		endr 
	endif 
	return 
endf 

def node_004
	if self.targetEnemy(target_em.any_monster) 
		random (20) >> Global.node_063 
		elser (80) 
		endr 
	endif 
	random (60) >> Global.node_068 
	elser (25) >> Global.node_076 
	elser (15) >> Global.node_053 
	endr 
	return 
endf 

