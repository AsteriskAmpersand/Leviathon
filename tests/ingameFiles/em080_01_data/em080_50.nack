importactions Acidic_Glavenus as acidic_glavenus

def node_000
	self.targetEnemy(target_em.closest_entity) 
	if self.target_is(em.Rathalos) 
		>> node_001 
	elif self.target_is(em.Azure_Rathalos) 
		>> node_001 
	elif self.target_is(em.Zinogre) 
		>> node_003 
	elif self.target_is(em.Diablos) 
		>> node_002 
	elif self.target_is(em.Black_Diablos) 
		>> node_002 
	elif self.target_is(em.Tigrex) 
		>> node_004 
	elif self.target_is(em.Brute_Tigrex) 
		>> node_004 
	elif self.target_is(em.Odogaron) 
		>> node_005 
	elif self.target_is(em.Ebony_Odogaron) 
		>> node_005 
	endif 
	conclude 
endf 

def node_001
	if self.distance_2d_to_target().gt(1900) 
		-> acidic_glavenus.dash(6,0,0,1800) 
	endif 
	if self.distance_2d_to_target().leq(1100) 
		-> acidic_glavenus.back_turn() 
	else 
		-> acidic_glavenus.quick_turn() 
	endif 
	-> acidic_glavenus.grapple_with_em002_ver2_start() 
	return 
endf 

def node_002
	if self.distance_2d_to_target().gt(1800) 
		-> acidic_glavenus.dash(6,0,0,1770) 
	endif 
	if self.distance_2d_to_target().leq(1700) 
		-> acidic_glavenus.back_turn() 
		-> acidic_glavenus.dash(1,0,0,1770) 
	else 
		-> acidic_glavenus.quick_turn() 
	endif 
	-> acidic_glavenus.grapple_with_em007_start() 
	return 
endf 

def node_003
	if self.distance_2d_to_target().gt(1600) 
		-> acidic_glavenus.dash(6,0,0,1500) 
	endif 
	-> acidic_glavenus.grapple_with_em057_first() 
	return 
endf 

def node_004
	if self.distance_3d_to_target().gt(1600) 
		-> acidic_glavenus.dash(1,0,0,1500) 
	endif 
	-> acidic_glavenus.grapple_with_em032_const() 
	return 
endf 

def node_005
	if self.distance_2d_to_target().gt(1600) 
		-> acidic_glavenus.dash(6,0,0,1500) 
	endif 
	-> acidic_glavenus.grapple_with_em113_first() 
	return 
endf 

