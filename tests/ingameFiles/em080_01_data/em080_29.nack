importactions Acidic_Glavenus as acidic_glavenus
importlibrary Global as Global

def node_000 : 1
	>> Global.node_096 
	if function#41() 
		>> Global.node_020 => reset 
	else 
	endif 
	>> Global.node_001 
	>> Global.node_021 
	>> node_001 => reset 
	reset 
endf 

def node_001 : 2
	self.targetEnemy(48) 
	if self.in_map(st.Challenge_Arena).in_area(-1) 
		>> node_004 
	elif self.distance_3d_to_target().leq(3500) 
		>> node_002 
	else 
		>> node_003 
	endif 
	return 
endf 

def node_002 : 3
	>> Global.node_008 
	if self.fatigued() 
		-> acidic_glavenus.tired_wait() 
	else 
		random (40) -> acidic_glavenus.wait() 
		elser (60) -> acidic_glavenus.search() 
		endr 
	endif 
	return 
endf 

def node_003 : 4
	if self.targetEnemy(target_em.any_monster) 
		random (20) >> Global.node_008 
		elser (80) 
		endr 
	else 
	endif 
	random (20) -> acidic_glavenus.combat_search() 
	elser (35) -> acidic_glavenus.anger_search() 
	elser (10) -> acidic_glavenus.short_search() 
	elser (20) -> acidic_glavenus.search() 
	elser (15) -> acidic_glavenus.wait() 
	endr 
	return 
endf 

def node_004 : 23
	>> Global.node_008 
	random (30) -> acidic_glavenus.wait() 
	elser (25) -> acidic_glavenus.short_search() 
	elser (10) -> acidic_glavenus.search() 
	elser (15) -> acidic_glavenus.combat_search() 
	elser (20) -> acidic_glavenus.threat() 
	endr 
	return 
endf 

