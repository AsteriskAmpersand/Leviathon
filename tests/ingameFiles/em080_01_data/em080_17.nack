importactions Acidic_Glavenus as acidic_glavenus
importlibrary Global as Global

def node_000
	>> Global.node_096 
	>> node_001 
	>> node_002 
	conclude 
endf 

def node_001
	if function#41() 
	else 
		if function#AB() 
			function#AA() 
			-> acidic_glavenus.threat() 
		else 
		endif 
	endif 
	return 
endf 

def node_002
	self.targetArea(14) 
	if self.distance_3d_to_target().gt(800) 
		>> node_004 
		repeat 
	else 
	endif 
	return 
endf 

def node_003
	self.targetEnemy(target_em.random_player_or_cat) 
	-> acidic_glavenus.idle() 
	if self.distance_3d_to_target().leq(800) 
		-> acidic_glavenus.back_turn() 
	elif self.angle_2d_cw_between(30,330) 
		-> acidic_glavenus.turn() 
	else 
	endif 
	return 
endf 

def node_004
	if self.fatigued() 
		-> acidic_glavenus.tired_dash(2,5,0,3000) 
	else 
		-> acidic_glavenus.dash(2,5,0,3000) 
	endif 
	return 
endf 

